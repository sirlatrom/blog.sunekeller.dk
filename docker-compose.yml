version: '2'

services:
  app:
    image: jekyll/jekyll
    command: jekyll serve --future --drafts
    volumes:
      - ./:/srv/jekyll
      - ./vendor/bundle:/usr/local/bundle
    ports:
      - 4000:4000
